file	problemtype.bas
where	*# Point Element Blocks

*if(CondNumEntities > 0)
Begin Elements <NAME>
*loop nodes *OnlyInCond
*set var ielem=operation(ielem+1)
*format "%i%i%i"
*ielem 0 *NodesNum
*end nodes
End Elements

*endif

