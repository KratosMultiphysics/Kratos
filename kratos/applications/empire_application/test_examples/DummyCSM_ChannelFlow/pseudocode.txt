/*
 * ================================================
 * Pseudo code of the server (EMPEROR)
 * ================================================
 */
int main() {
	// Stage 1: receive meshes ...
	// Stage 2: do co-simulation ...
	//  ...
	//  ...
	for (int i=0; i<numberOfTimeSteps; i++) {// time step loop
		//  ...
		while (!isConvergent) { // iterative coupling loop
			//  ...
			sendSignalTo("subSystem2", "interfaceDisplacementU");
			//  ...
			//  ...
			receiveSignalFrom("subSystem2", "interfaceForceY");
			sendSignalTo("subSystem1", "interfaceForceU");
			//  ...
			//  ...
			receiveSignalFrom("subSystem1", "interfaceDisplacementY");
			//  ...
			sendConvergenceSignalTo(subSystem1, isConvergent);
			sendConvergenceSignalTo(subSystem2, isConvergent);
		}
	}
	//  ...
	return;
}



/*
 * ================================================
 * Pseudo code of the client code (subSystem1)
 * ================================================
 */
int main() {
	// Stage 1: send meshes ...
	// Stage 2: do co-simulation ...
	for (int i=0; i<numberOfTimeSteps; i++) {// time step loop
		//  ...
		while (!isConvergent) { // iterative coupling loop
			//  ...
			receiveSignalFromServer("interfaceForceU");
			//  ...
			//  ...
			sendSignalToServer("interfaceDisplacementY");
			//  ...
			isConvergent = receiveConvergenceSignalFromServer();
		}
	}
	//  ...
	return;
}



/*
 * ================================================
 * Pseudo code of the client code (subSystem2)
 * ================================================
 */
int main() {
	// Stage 1: send meshes ...
	// Stage 2: do co-simulation ...
	for (int i=0; i<numberOfTimeSteps; i++) {// time step loop
		//  ...
		while (!isConvergent) { // iterative coupling loop
			//  ...
			receiveSignalFromServer("interfaceDisplacementU");
			//  ...
			//  ...
			sendSignalToServer("interfaceForceY");
			//  ...
			isConvergent = receiveConvergenceSignalFromServer();
		}
	}
	//  ...
	return;
}





