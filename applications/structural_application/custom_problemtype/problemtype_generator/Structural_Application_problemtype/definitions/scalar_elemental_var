file	problemtype.bas
where	*# Elemental Variable blocks

*if(CondNumEntities > 0)
Begin ElementalData <NAME>
*loop elems *OnlyInCond
*format "%i%f"
*ElemsNum *cond(<NAME>)
*end nodes
End ElementalData

*endif

